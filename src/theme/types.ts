export interface IColor {
  blue: string;
  red: string;
  text_second: string;
  lightGrey: string;
  green: string;
  text: string;
  yellow: string;
  disable: string;
  form_background: string;
}
